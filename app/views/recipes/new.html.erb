<% content_for :tab, :recipe %>
<% content_for :content do %>
    <div class="card">
      <div class="card-header text-white bg-secondary">
        <h3>Ajout d'une recette</h3>
      </div>
      <div class="card-body">
        <%= render 'form', recipe: @recipe %>
      </div>
    </div>
<% end %>
<%= render template: 'layouts/profile' %>